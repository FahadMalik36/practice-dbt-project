[project]
name = "practice-dbt-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "fahadrazaqmalik@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "dbt-core>=1.12.0",
    "dbt-databricks>=1.12.3",
]

[project.scripts]
practice-dbt-project = "practice_dbt_project:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
